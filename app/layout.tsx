import type { Metadata, Viewport } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import BaoVeLoi from '@/app/components/BaoVeLoi';
import GlobalCodeEditor from '@/app/components/GlobalCodeEditor';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Hệ Thống Quản Lý ERP',
  description: 'Generated by Tommy Nghiem',
  manifest: '/manifest.json',
  
  // 🟢 Cấu hình PWA/Apple chuẩn Next.js (Thay thế cho các thẻ <meta> thủ công)
  appleWebApp: {
    capable: true,
    statusBarStyle: 'black-translucent',
  },
  icons: {
    apple: '/icon-192.png', // Tự động tạo thẻ <link rel="apple-touch-icon">
  },
};

// 🟢 Cấu hình Viewport chuẩn (Quan trọng cho Mobile)
export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false, // Chặn zoom (user-scalable=no)
  themeColor: '#000000',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="vi">
      {/* 🟢 KHÔNG CẦN THẺ <head> THỦ CÔNG NỮA */}
      {/* Next.js sẽ tự động sinh thẻ <head> dựa trên metadata và viewport ở trên */}
      
      <body className={inter.className}>
        {/* Wrapper full height */}
        <div className="min-h-screen w-full bg-black">
          <BaoVeLoi>{children}</BaoVeLoi>
        </div>
        <GlobalCodeEditor />
      </body>
    </html>
  );
}