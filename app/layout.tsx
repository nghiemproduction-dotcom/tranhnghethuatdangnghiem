import type { Metadata, Viewport } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import BaoVeLoi from '@/app/components/BaoVeLoi';
import GlobalCodeEditor from '@/app/components/GlobalCodeEditor';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Hệ Thống Quản Lý ERP',
  description: 'Generated by Tommy Nghiem',
  manifest: '/manifest.json',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: '#000000',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="vi">
      <head>
        {/* Đảm bảo 100% mobile tôn trọng viewport */}
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
        
        {/* PWA fullscreen trên iOS */}
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
        
        {/* Link manifest & theme color */}
        <link rel="manifest" href="/manifest.json" />
        <meta name="theme-color" content="#000000" />
        
        {/* Icon cho Apple */}
        <link rel="apple-touch-icon" href="/icon-192.png" />
      </head>
      
      {/* BỎ fixed + overflow-hidden KHỎI BODY → tránh bug 100vh & address bar trên iOS */}
      <body className={inter.className}>
        {/* Wrapper full height để children tự do dùng 100dvh như hiện tại */}
        <div className="min-h-screen w-full bg-black">
          <BaoVeLoi>{children}</BaoVeLoi>
        </div>
        <GlobalCodeEditor />
      </body>
    </html>
  );
}