import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

// Import các thành phần tiện ích (Giữ nguyên)
import BaoVeLoi from '@/app/components/BaoVeLoi';
import GlobalCodeEditor from '@/app/components/GlobalCodeEditor';

// ❌ ĐÃ BỎ: ForceFullScreen (Vì đã chuyển vào CongDangNhap)
// ❌ ĐÃ BỎ: MenuTren, MenuDuoi (Vì đã chuyển vào DashboardBuilder)

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Hệ Thống Quản Lý ERP',
  description: 'Generated by Tommy Nghiem',
  
  // 🟢 Vẫn giữ dòng này để ai cài App trên điện thoại thì tự động Fullscreen xịn
  manifest: '/manifest.json', 
  themeColor: '#1A1A1A',
  viewport: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="vi">
      <body className={inter.className}>
        
        {/* Chỉ bọc bảo vệ lỗi */}
        <BaoVeLoi>
            {children}
        </BaoVeLoi>

        {/* Công cụ sửa code (Giữ nguyên) */}
        <GlobalCodeEditor />
        
      </body>
    </html>
  );
}