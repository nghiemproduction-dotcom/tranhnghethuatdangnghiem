import type { Metadata, Viewport } from 'next';
import { Inter } from 'next/font/google';
import './globals.css'; 
import ForceFullScreen from '@/app/components/ForceFullScreen';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Hệ Thống Quản Lý ERP',
  description: 'Generated by Tommy Nghiem',
  manifest: '/manifest.json',
  
  // 🟢 CẤU HÌNH ICON PWA CHUẨN
  icons: {
    icon: '/icon-192.png',           // Icon cho tab trình duyệt & Android
    shortcut: '/icon-192.png',
    apple: '/icon-192.png',          // Icon cho iOS Home Screen
    other: {
      rel: 'apple-touch-icon-precomposed',
      url: '/icon-192.png',
    },
  },

  appleWebApp: {
    capable: true,
    statusBarStyle: 'black-translucent',
    title: 'ArtSpace ERP',
  },
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  viewportFit: 'cover', 
  themeColor: '#000000',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="vi">
      <body className={`${inter.className} bg-black overflow-hidden`}>
        <ForceFullScreen />
        <main className="w-full h-full relative flex flex-col">
            {children}
        </main>
      </body>
    </html>
  );
}