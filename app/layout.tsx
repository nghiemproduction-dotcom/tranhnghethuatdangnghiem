import type { Metadata, Viewport } from 'next'; // 🟢 Thêm import Viewport
import { Inter } from 'next/font/google';
import './globals.css';

// Import các thành phần tiện ích (Giữ nguyên)
import BaoVeLoi from '@/app/components/BaoVeLoi';
import GlobalCodeEditor from '@/app/components/GlobalCodeEditor';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Hệ Thống Quản Lý ERP',
  description: 'Generated by Tommy Nghiem',
  manifest: '/manifest.json', // Giữ nguyên PWA
};

// 🟢 TÁCH VIEWPORT RA ĐÂY (QUAN TRỌNG CHO MOBILE)
export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false, // Chặn zoom bằng 2 ngón tay -> Giống App Native
  themeColor: '#1A1A1A',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="vi">
      <body className={inter.className}>
        <BaoVeLoi>
            {children}
        </BaoVeLoi>
        <GlobalCodeEditor />
      </body>
    </html>
  );
}