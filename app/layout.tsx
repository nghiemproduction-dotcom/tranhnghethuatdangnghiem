import type { Metadata, Viewport } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import BaoVeLoi from '@/app/components/BaoVeLoi';
import GlobalCodeEditor from '@/app/components/GlobalCodeEditor';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Hệ Thống Quản Lý ERP',
  description: 'Generated by Tommy Nghiem',
  manifest: '/manifest.json',
  // ❌ TUYỆT ĐỐI KHÔNG ĐỂ viewport Ở ĐÂY NỮA
};

// 🟢 CẤU HÌNH MOBILE BẮT BUỘC (NẰM RIÊNG)
export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false, // Cấm zoom
  themeColor: '#000000',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="vi">
      <body className={`${inter.className} bg-black overflow-hidden fixed inset-0 w-full h-full`}>
        <BaoVeLoi>{children}</BaoVeLoi>
        <GlobalCodeEditor />
      </body>
    </html>
  );
}